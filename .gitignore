node_modules/
dist/
coverage/
.env
.env.local
.env.*.local
*.log
*.tsbuildinfo
.DS_Store

# Vercel CLI's local project-link metadata (org/project IDs) — set as
# GitHub secrets instead, see the plan's Phase 7.4/7.5.
.vercel

# Scratch/draft input, not source of truth — see the implementation plan's
# Context section for why (superseded by the real schema/migrations).
tmp/
.env*
